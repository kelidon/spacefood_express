typedef UIC = _UiConstants;

class _UiConstants {
  static const Duration pageAnimationDuration = Duration(milliseconds: 700);
  static const Duration commonAnimationDuration = Duration(milliseconds: 200);
}
